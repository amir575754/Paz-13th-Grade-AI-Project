package com.apps.paz.bullseye.enums;

public enum ColorEnum { //An enum which represents each of the different colors a code in the game can have.
    Red,
    Pink,
    Green,
    Blue,
    Orange,
    Yellow
}
